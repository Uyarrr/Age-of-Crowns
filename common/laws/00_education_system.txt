law_uneducated_masses = {
	group = lawgroup_education_system

	icon = "gfx/interface/icons/law_icons/uneducated_masses.dds"

	progressiveness = -25

	disallowing_laws = {
	}

	modifier = {
		state_aristocrats_education_access_add = 0.10
		state_clergymen_education_access_add = 0.10
		state_clerks_education_access_add = 0.10
		country_weekly_innovation_mult = -0.10
        building_group_bg_agriculture_allowed_collectivization_add = -0.05
        building_group_bg_ranching_allowed_collectivization_add = -0.05
		country_leverage_resistance_mult = 0.10
	}

	can_enact = {
		custom_tooltip = {
			modifier:country_disallow_law_no_schools_bool = no
			text = disallow_law_no_schools_tt
		}
	}

	# AI will never abolish institutions entirely
	ai_will_do = {
		always = no
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_religious_teachings = {
	group = lawgroup_education_system

	icon = "gfx/interface/icons/law_icons/religious_teachings.dds"

	progressiveness = 0

	unlocking_technologies = {
		religious_schools
	}

	disallowing_laws = {
	}

	modifier = {
		state_aristocrats_education_access_add = 0.05
		state_clergymen_education_access_add = 0.05
		state_clerks_education_access_add = 0.05
		state_education_access_add = 0.05

		# Spiritualism 0.10 #
		state_assimilation_mult = -0.10
		state_conversion_mult = 0.10
		country_acceptance_no_shared_religious_trait_add = -12
		country_acceptance_shared_religious_trait_add = 6
		country_acceptance_shared_religious_trait_group_add = 6
	}

	institution = institution_schools
	institution_modifier = {
		state_assimilation_mult = -0.05
		state_conversion_mult = 0.05
		state_education_access_add = 0.05
		interest_group_ig_devout_pol_str_mult = 0.1
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_education_system_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				has_law_or_variant = law_type:law_uneducated_masses
			}
			add = 500
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_religious_teachings
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = base_impose_law_weight
		}

		if = {
			limit = {
				has_law = law_type:law_religious_teachings
				scope:target_country = {
					has_law_or_variant = law_type:law_uneducated_masses
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = 10
		}
	}
}

law_devout_education = {
	group = lawgroup_education_system

	icon = "gfx/interface/icons/law_icons/devout_education.dds"

	progressiveness = 25

	unlocking_technologies = {
		behaviorism
	}

	unlocking_laws = {
		law_theocracy
	}

	disallowing_laws = {
	}

	modifier = {
		state_aristocrats_education_access_add = 0.05
		state_clergymen_education_access_add = 0.05
		state_clerks_education_access_add = 0.05
		state_education_access_add = 0.05

		# Spiritualism 0.05 #
		state_assimilation_mult = -0.05
		state_conversion_mult = 0.05
		country_acceptance_no_shared_religious_trait_add = -6
		country_acceptance_shared_religious_trait_add = 3
		country_acceptance_shared_religious_trait_group_add = 3

		# Traditionalism 0.10 #
		country_weekly_innovation_mult = -0.10
		country_leverage_resistance_mult = 0.10
	}

	institution = institution_schools
	institution_modifier = {
		state_education_access_wealth_add = 0.005
		interest_group_ig_intelligentsia_pol_str_mult = 0.1
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_education_system_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				has_law_or_variant = law_type:law_uneducated_masses
			}
			add = 500
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_devout_education
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = base_impose_law_weight
		}

		if = {
			limit = {
				has_law = law_type:law_devout_education
				scope:target_country = {
					has_law_or_variant = law_type:law_uneducated_masses
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = 10
		}
	}
}

law_individual_apprenticeships = {
	group = lawgroup_education_system

	icon = "gfx/interface/icons/invention_icons/apprenticeship_system.dds"

	progressiveness = 50

	unlocking_technologies = {
		behaviorism
	}

	disallowing_laws = {
	}

	institution = institution_schools
	institution_modifier = {
		state_education_access_add = 0.05
		state_trade_advantage_mult = 0.10
		state_pop_qualifications_mult = 0.05
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_education_system_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				has_law_or_variant = law_type:law_uneducated_masses
			}
			add = 500
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_individual_apprenticeships
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = base_impose_law_weight
		}

		if = {
			limit = {
				has_law = law_type:law_individual_apprenticeships
				scope:target_country = {
					has_law_or_variant = law_type:law_uneducated_masses
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = 10
		}
	}
}

law_oral_tradition = {
	group = lawgroup_education_system

	icon = "gfx/interface/icons/law_icons/oral_tradition.dds"

	progressiveness = 50

	unlocking_technologies = {
		behaviorism
	}

	disallowing_laws = {
		law_state_religion
		law_serfdom
	}

	modifier = {
		political_movement_radicalism_add = -0.05
		country_legitimacy_base_add = 5

		# Spiritualism 0.05 #
		state_assimilation_mult = -0.05
		state_conversion_mult = 0.05
		country_acceptance_no_shared_religious_trait_add = -6
		country_acceptance_shared_religious_trait_add = 3
		country_acceptance_shared_religious_trait_group_add = 3
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_education_system_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				has_law_or_variant = law_type:law_no_schools
			}
			add = 500

			if = {
				limit = {
					OR = {
						has_journal_entry = je_sick_man_education
						has_journal_entry = je_terakoya
						has_journal_entry = je_portugal_regeneration_institutions
					}
				}
				add = 500
			}
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_public_schools
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = base_impose_law_weight
		}

		if = {
			limit = {
				has_law = law_type:law_public_schools
				scope:target_country = {
					has_law_or_variant = law_type:law_no_schools
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = 10
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

law_personal_religious_education = {
	group = lawgroup_education_system

	icon = "gfx/interface/icons/law_icons/public_schools.dds"

	progressiveness = 50

	unlocking_technologies = {
		behaviorism
	}

	disallowing_laws = {
		law_state_religion
		law_serfdom
	}

	institution = institution_schools
	institution_modifier = {
		state_assimilation_mult = 0.125
		state_education_access_add = 0.125
		country_education_fervor_add = 5
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_education_system_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				has_law_or_variant = law_type:law_no_schools
			}
			add = 500

			if = {
				limit = {
					OR = {
						has_journal_entry = je_sick_man_education
						has_journal_entry = je_terakoya
						has_journal_entry = je_portugal_regeneration_institutions
					}
				}
				add = 500
			}
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_public_schools
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = base_impose_law_weight
		}

		if = {
			limit = {
				has_law = law_type:law_public_schools
				scope:target_country = {
					has_law_or_variant = law_type:law_no_schools
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = 10
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

law_epic_tradition = {
	group = lawgroup_education_system

	icon = "gfx/interface/icons/law_icons/public_schools.dds"

	progressiveness = 50

	unlocking_technologies = {
		behaviorism
	}

	disallowing_laws = {
		law_state_religion
		law_serfdom
	}

	institution = institution_schools
	institution_modifier = {
		state_assimilation_mult = 0.125
		state_education_access_add = 0.125
		country_education_fervor_add = 5
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_education_system_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				has_law_or_variant = law_type:law_no_schools
			}
			add = 500

			if = {
				limit = {
					OR = {
						has_journal_entry = je_sick_man_education
						has_journal_entry = je_terakoya
						has_journal_entry = je_portugal_regeneration_institutions
					}
				}
				add = 500
			}
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_public_schools
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = base_impose_law_weight
		}

		if = {
			limit = {
				has_law = law_type:law_public_schools
				scope:target_country = {
					has_law_or_variant = law_type:law_no_schools
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = 10
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

law_secular_education = {
	group = lawgroup_education_system

	icon = "gfx/interface/icons/law_icons/public_schools.dds"

	progressiveness = 50

	unlocking_technologies = {
		behaviorism
	}

	disallowing_laws = {
		law_state_religion
		law_serfdom
	}

	institution = institution_schools
	institution_modifier = {
		state_assimilation_mult = 0.125
		state_education_access_add = 0.125
		country_education_fervor_add = 5
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_education_system_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				has_law_or_variant = law_type:law_no_schools
			}
			add = 500

			if = {
				limit = {
					OR = {
						has_journal_entry = je_sick_man_education
						has_journal_entry = je_terakoya
						has_journal_entry = je_portugal_regeneration_institutions
					}
				}
				add = 500
			}
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_public_schools
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = base_impose_law_weight
		}

		if = {
			limit = {
				has_law = law_type:law_public_schools
				scope:target_country = {
					has_law_or_variant = law_type:law_no_schools
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = 10
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}

law_vedic_education = {
	group = lawgroup_education_system

	icon = "gfx/interface/icons/law_icons/public_schools.dds"

	progressiveness = 50

	unlocking_technologies = {
		behaviorism
	}

	disallowing_laws = {
		law_state_religion
		law_serfdom
	}

	institution = institution_schools
	institution_modifier = {
		state_assimilation_mult = 0.125
		state_education_access_add = 0.125
		country_education_fervor_add = 5
	}

	can_impose = {
		OR = {
			AND = {
				is_in_same_power_bloc = scope:target_country
				AND = {
					modifier:country_can_impose_same_lawgroup_education_system_in_power_bloc_bool = yes
					has_law = scope:law
				}
			}
			can_impose_law_default = yes
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}

		if = {
			limit = {
				has_law_or_variant = law_type:law_no_schools
			}
			add = 500

			if = {
				limit = {
					OR = {
						has_journal_entry = je_sick_man_education
						has_journal_entry = je_terakoya
						has_journal_entry = je_portugal_regeneration_institutions
					}
				}
				add = 500
			}
		}
	}

	ai_impose_chance = {
		value = 0

		if = {
			limit = {
				has_law = law_type:law_public_schools
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = base_impose_law_weight
		}

		if = {
			limit = {
				has_law = law_type:law_public_schools
				scope:target_country = {
					has_law_or_variant = law_type:law_no_schools
				}
				power_bloc ?= { has_principle_group = principle_group:principle_group_advanced_research }
			}
			add = 10
		}

		if = {
			limit = {
				scope:target_country = {
					c:BIC ?= this
					OR = {
						has_journal_entry = je_british_dictate_plantations
						has_journal_entry = je_british_dictate_military
						has_journal_entry = je_british_dictate_universities
					}
				}
			}
			multiply = 0
		}
	}
}